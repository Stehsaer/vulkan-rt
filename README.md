# Vulkan RT

Personal experimental vulkan ray-tracing project. Currently WIP.

## Prerequisites

- C++23 compatible compiler
  > Note: For GCC, use GCC 15 or later, GCC 14 doesn't work

- Vulkan Header (from `apt` or from Vulkan SDK)
- Slang Compiler (https://github.com/shader-slang/slang/releases)